<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
      <!--Validación del Genero -->
    <%if (infoGeneros.length===0) {%>
        <h1>
            <p>No se encuentra ninguna banda en el género ingresado. Prueba con otro.</p>
        </h1>

        <% }else { %>

            <ol>
                <% for( let i=0; i < infoGeneros.length; i++ ) { %>
                    <li>
                        <p><strong>Banda :</strong>
                            <!--Con esta ruta va al detalle de cada banda que esta en ese genero-->
                            <a href="/bandas/id/<%= infoGeneros[i].id %>">
                                <%= infoGeneros[i].nombre %>,
                            </a>

                        </p>
                    </li>
                    <% } %>
            </ol>

        <% } %>

</body>

</html>